.center
    div(class='page main-menu' hx-target='.content' hx-vals=`{"playerId": "${player.playerId}"}`)  
        h1(class='title' id='game-title') Answer Wager
        button(id='join-game' class='menu-button' hx-get=`/games/join` hx-prompt='Enter Game ID: ') Join Game 
        button(id='host-game' class='menu-button' hx-get=`/games/${player.playerId}/create`) Host Game
        button(id='game-rules' class='menu-button' hx-get=`/game-rules/${player.playerId}`) Game Rules

