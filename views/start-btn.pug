if playersReady
 div(hx-swap='outerHTML' hx-trigger='every 5s' hx-get=`/games/${gameId}/readycheck` hx-include="[name='gameId'],[name='playerId']")
  button(type='button' id='start-btn' class='menu-btn' hx-swap='outerHTML' hx-get=`/games/${gameId}/start` hx-include="[name='gameId'],[name='playerId']") Start
else 
 button(type='button' id='start-btn' class='menu-btn' hx-swap='outerHTML' hx-trigger='every 5s' hx-get=`/games/${gameId}/readycheck` hx-include="[name='gameId'],[name='playerId']" disabled) Start